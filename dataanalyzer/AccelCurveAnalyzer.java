package dataanalyzer;

import storage.Reader;
import storage.Writer;

public class AccelCurveAnalyzer extends DataAnalyzer {
    public AccelCurveAnalyzer(Reader reader, Writer writer) {
        super(reader, writer);
    }

    public String analyze(String[] data) {
        // sorts the data
        return "";
}
